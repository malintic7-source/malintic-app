services:
  - type: web
    name: malintic-api
    env: docker
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
      - key: DATA_DIR
        value: /data
      - key: BOOTSTRAP_ADMIN_EMAIL
        value: admin@example.com
        sync: false
      - key: BOOTSTRAP_ADMIN_PASSWORD
        value: ChangeMe123!
        sync: false
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
